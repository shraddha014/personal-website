import styles from './experience.module.css';
import { neue } from '@/utils/fontConfig';

export default function Experience() { 
    return (
        <div className={`${styles.mainContainer} ${neue.className}`}>Coming Soon...</div>
    );
 }